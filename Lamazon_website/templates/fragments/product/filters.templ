package productfrag

// filters.templ — placeholder; active filters are rendered inline.
// Reserved for a future active-filter-chips HTMX fragment.

templ Filters() {
	<!-- filters fragment placeholder -->
}
